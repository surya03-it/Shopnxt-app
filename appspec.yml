version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::Service
      Properties:
        TaskDefinition: <TASK_DEFINITION>
        LoadBalancerInfo:
          ContainerName: "shopnxt-container"
          ContainerPort: 5000
Hooks:
  - AfterAllowTestTraffic:
      - location: "scripts/validate.sh"
        timeout: 60
